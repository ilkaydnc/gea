# Router Simple

The simplest possible routing example with Gea. Demonstrates flat route definitions using `createRouter` from `gea-router` — no nested layouts, no guards, just paths mapped to components. Includes active link highlighting with `router.isActive()` and dynamic `:id` params.
